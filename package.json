{
  "name": "job-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension + dashboard for tracking job applications",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:extension": "pnpm --filter extension dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=24.0.0"
  }
}
